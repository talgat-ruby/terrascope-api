[project]
name = "terrascope-cli2"
version = "0.1.0"
description = "Terrascope cli2 — per-process pipeline (experimental)"
requires-python = ">=3.14"
dependencies = [
    "terrascope-core",
    "terrascope-core2",
    "typer>=0.12",
    "shapely>=2.0",
]

[project.scripts]
terrascope2 = "cli2.main:app"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/cli2"]
